# DEX Quant Skill — 完整链路测试场景

安装完成后，在 Codex 命令行中逐个测试以下场景。
每个场景包含：用户输入 → 预期 AI 行为 → 验证点。

---

## 场景 1：从零创建策略 + 本地回测（核心链路）

### 对话流程

```
用户: 帮我做一个 ETH 的策略，RSI 低于 30 就买，高于 70 就卖
```

### 预期行为

1. AI 读取 `strategy-maker/SKILL.md`
2. 信息完整（币种=ETH，买入=RSI<30，卖出=RSI>70），无需追问
3. 生成脚本保存到 `strategies/eth_rsi_strategy.py`
4. 告知用户：
   - 脚本保存路径
   - 策略说明（RSI 超卖买入 / 超买卖出）
   - 运行命令

### 验证点

- [ ] 脚本文件在 `strategies/` 目录下
- [ ] 脚本包含 `generate_signals()` 函数
- [ ] 脚本输出符合标准信号 JSON 格式
- [ ] SKILL_ROOT 路径指向上级目录 `..`

### 继续对话

```
用户: 帮我回测 2024 全年
```

### 预期行为

1. AI 读取 `backtester/SKILL.md`
2. 本地运行脚本生成信号
3. 将信号发送到服务器回测
4. 展示绩效报告（收益率、Sharpe、回撤、胜率等）
5. 根据结论给出下一步建议

### 验证点

- [ ] 信号成功生成（非空）
- [ ] 服务器回测返回完整结果
- [ ] 结论为 approved / paper_trade_first / rejected 之一

---

## 场景 2：需要追问的策略创建

### 对话流程

```
用户: 帮我做一个 MACD 金叉就买的策略
```

### 预期行为

1. AI 发现缺少必要信息：
   - 目标币种未指定
   - 卖出条件未指定
2. AI 追问这两个问题

### 用户回答

```
用户: BTC，死叉就卖
```

### 预期行为

1. 信息补全，生成策略脚本
2. 保存到 `strategies/` 目录

### 验证点

- [ ] AI 没有用默认值替代必须追问的信息
- [ ] 追问后能正确生成脚本

---

## 场景 3：多条件组合策略

### 对话流程

```
用户: 帮我做一个 SOL 的策略，布林带下轨 + RSI 低于 35 + 成交量放大就买，布林带上轨或者 RSI 高于 70 就卖，15 分钟周期
```

### 预期行为

1. 识别出多个条件的 AND / OR 组合
2. 生成脚本使用布林带、RSI、成交量均线三个指标
3. 买入条件: 布林带下轨 AND RSI<35 AND 放量
4. 卖出条件: 布林带上轨 OR RSI>70
5. 周期设为 15m

### 验证点

- [ ] 脚本中同时计算了 Bollinger Bands、RSI、volume_ma
- [ ] 买入条件是 AND 组合
- [ ] 卖出条件是 OR 组合
- [ ] TIMEFRAME = "15m"

---

## 场景 4：查看已有策略

### 对话流程

```
用户: 我有哪些策略
```

### 预期行为

1. AI 扫描 `strategies/` 目录
2. 列出所有策略脚本及其核心信息（名称、币种、周期、逻辑）

### 验证点

- [ ] 列出了所有 `strategies/*.py` 文件
- [ ] 每个策略有名称、币种、周期、买卖逻辑摘要

---

## 场景 5：修改已有策略

### 对话流程

```
用户: 把 btc_macd_vol_strategy 的止损改成 2%，放量倍数改成 1.2
```

### 预期行为

1. 读取 `strategies/btc_macd_vol_strategy.py`
2. 修改 `STOP_LOSS_PCT = 0.02` 和 `VOL_MULTIPLIER = 1.2`
3. 告知用户修改了哪些参数

### 验证点

- [ ] 只改了指定参数，没有改动策略逻辑
- [ ] 修改后脚本仍然可以正常运行

### 继续对话

```
用户: 重新回测一下 2024 年看看
```

### 验证点

- [ ] 使用修改后的脚本重新回测
- [ ] 结果与之前不同（参数变了）

---

## 场景 6：做多做空双向策略

### 对话流程

```
用户: BTC 做个技术指标趋势策略，均线金叉做多死叉做空，1h
```

### 预期行为

1. 生成支持做多和做空的策略
2. 金叉时信号 direction="long"
3. 死叉时信号 direction="short"
4. 止盈止损根据方向正确设置

### 验证点

- [ ] 脚本输出的信号包含 `direction` 字段
- [ ] 做多止损在入场价下方，做空止损在入场价上方
- [ ] TIMEFRAME = "1h"

---

## 场景 7：不同回测时间段对比

### 对话流程

```
用户: 把 btc_macd_vol_strategy 分别跑 2023 年和 2024 年的回测，对比一下
```

### 预期行为

1. 分别以 2023-01-01~2023-12-31 和 2024-01-01~2024-12-31 运行回测
2. 两次回测结果对比展示
3. 分析策略在不同市场环境下的表现差异

### 验证点

- [ ] 两次回测都成功返回结果
- [ ] 有对比分析（哪个时段更好、稳健性评估）

---

## 场景 8：回测不通过后优化

### 对话流程

```
用户: 帮我做一个 DOGE 的策略，KDJ 金叉就买，死叉就卖，5 倍杠杆
```

### 预期行为

1. 生成 DOGE KDJ 策略脚本

### 继续对话

```
用户: 回测一下 2024 年
```

假设回测结果不理想（被驳回或建议先模拟）：

```
用户: 帮我优化一下
```

### 预期行为

1. AI 分析回测报告中的问题（胜率低？回撤大？）
2. 给出具体优化建议（加过滤条件？调参数？）
3. 修改脚本并重新回测

### 验证点

- [ ] AI 能读懂回测结果并分析问题
- [ ] 优化后重新回测，结果有改善

---

## 场景 9：策略脚本独立运行验证

不通过 AI 对话，直接在终端验证脚本能跑。

### 命令

```bash
# 确认脚本存在
ls strategies/

# 直接运行脚本（会进入交互模式）
python3 strategies/btc_macd_vol_strategy.py backtest 2024-06-01 2024-12-31
```

### 预期行为

1. 显示平台介绍
2. 显示策略信息 + **脚本路径**
3. **第 1 步**: 本地生成信号，显示信号统计
4. **第 2 步**: 选择回测模式 [1] 本地回测 / [2] 服务器回测
5. **第 3 步**: 连接服务器，输入参数，执行回测
6. 显示绩效报告 + 交易记录
7. 显示**下一步操作**建议

### 验证点

- [ ] 步骤编号清晰（第 1/2/3 步）
- [ ] 有 ✓/✗ 状态标记
- [ ] 脚本路径正确显示
- [ ] 菜单只有 [1] 本地回测 和 [2] 服务器回测，没有本地导出
- [ ] 回测完成后有"下一步操作"提示

---

## 场景 10：错误处理

### 10a. 无效币种

```
用户: 帮我做一个 ABCXYZ 的 MACD 策略
```

验证: AI 应该提示这个币种可能不存在或无数据。

### 10b. 缺少日期范围

```bash
python3 strategies/btc_macd_vol_strategy.py backtest
```

验证: 应提示需要日期范围，并给出正确用法示例。

### 10c. 网络问题

```bash
# 断网后运行
python3 strategies/btc_macd_vol_strategy.py backtest 2024-01-01 2024-06-30
```

验证: 应给出友好的错误提示和解决建议（检查网络/设置代理）。

---

## 快速冒烟测试（5 分钟跑完）

如果时间有限，只跑这 3 个：

1. **场景 1** — 创建策略 + 回测（核心链路）
2. **场景 9** — 脚本独立运行（验证 runner 提示语）
3. **场景 4** — 查看已有策略（验证 strategies/ 目录）
